const { chromium } = require('playwright'); const fs = require('fs'); (async () => { const browser = await chromium.launch({ headless: false, channel: 'chrome' }); const context = await browser.newContext(); const page = await context.newPage(); await page.goto('https://supabase.com/dashboard/project/ryxdqnzyvnrwalylqyvm/sql/new'); console.log('Por favor inicia sesion manualmente en esta ventana si es necesario.'); await page.waitForTimeout(15000); const sql = fs.readFileSync('emergency_cleanup.sql', 'utf8'); try { await page.click('.monaco-editor'); await page.keyboard.press('Control+A'); await page.keyboard.press('Backspace'); await page.keyboard.insertText(sql); await page.click('button:has-text("Run")'); console.log('Ejecutado via UI!'); } catch(e) { console.log('Error UI:', e); } await page.waitForTimeout(5000); await browser.close(); })()
