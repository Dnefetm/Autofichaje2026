const { chromium } = require('playwright'); (async () => { const browser = await chromium.launch({ headless: false, channel: 'chrome' }); const context = await browser.newContext(); const page = await context.newPage(); await page.goto('https://supabase.com/dashboard/project/ryxdqnzyvnrwalylqyvm/sql/new'); console.log('Navegador abierto. Por favor pega el codigo y ejecutalo manualmente o dame tiempo para intentar hacerlo con la UI.'); })()
