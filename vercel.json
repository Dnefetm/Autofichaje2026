{
    "buildCommand": "npm run build --workspace=@gestor/dashboard",
    "outputDirectory": ".next",
    "framework": "nextjs",
    "installCommand": "npm install",
    "crons": [
        {
            "path": "/api/cron/dispatch-matching",
            "schedule": "* * * * *"
        }
    ]
}
