const { createClient } = require('@supabase/supabase-js'); const supabase = createClient('https://ryxdqnzyvnrwalylqyvm.supabase.co', process.env.SUPABASE_SERVICE_ROLE_KEY || process.env.SUPABASE_ANON_KEY); async function fix() { const { data, error } = await supabase.rpc('execute_sql', { sql: "TRUNCATE TABLE public.listas_precios_raw_staging; UPDATE public.importaciones_excel SET estado = 'error', error_mensaje = 'Limpiado via JS' WHERE estado IN ('procesando', 'mapeando');" }); console.log('Result:', data, error); } fix();
